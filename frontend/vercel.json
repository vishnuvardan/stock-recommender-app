{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://stock-recommender-app-backend.vercel.app/api/:path*"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
